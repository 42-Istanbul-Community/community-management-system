services:
  frontend:
    image: frontend
    container_name: frontend
    build: ../../services/frontend
    restart: always
    networks:
      - frontend
    environment:
      DOMAIN_NAME: ${DOMAIN_NAME}